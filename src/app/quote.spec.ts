import { Quote } from './quote';
import { QuoteFormComponent } from './quote-form/quote-form.component';

describe('Quote', () => {
  it('should create an instance', () => {
    expect(new QuoteFormComponent()).toBeTruthy();
  });
});
